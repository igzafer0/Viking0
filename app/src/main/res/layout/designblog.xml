<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="5dp"
    android:layout_marginRight="5dp"
    android:layout_marginBottom="5dp"
    android:layout_marginTop="10dp"
    android:id="@+id/rl"
    android:background="@drawable/rowstroke">


        <ImageView
            android:layout_width="match_parent"
            android:layout_height="100dp"
             android:id="@+id/foto"
            android:layout_margin="0.6dp"
            android:scaleType="centerCrop"
         />


    <TextView
        android:id="@+id/baslik"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/wavextrabold"
        android:text=""
        android:textSize="17sp"
        android:layout_marginTop="10dp"
        android:layout_below="@id/foto"
        android:layout_marginRight="5dp"
        android:layout_marginBottom="10dp"
        android:layout_marginLeft="5dp"
        android:textColor="@color/colorWhite" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_alignBottom="@id/baslik"
        android:layout_alignTop="@id/foto"
        android:layout_marginBottom="-10dp"
        style="@style/SelectableItemBackgroundButton"
        android:layout_height="wrap_content">

    </ImageView>

</RelativeLayout>